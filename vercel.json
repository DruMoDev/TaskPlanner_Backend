{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "set NODE_ENV=production&& @vercel/node",
            "config": { "zeroConfig": true }
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "index.js" }
    ]
}